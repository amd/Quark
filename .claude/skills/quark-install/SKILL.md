---
name: quark-install
description: >
  Install or verify the AMD Quark package and its dependencies. Trigger for "install Quark", "set up Quark",
  "pip install amd-quark", dependency errors, import failures for quark modules, ModuleNotFoundError for quark,
  or missing C++ compiler errors. For PyTorch installation or torch version issues, use quark-torch-install instead.
---

Read and follow the instructions in `.claude/skills-impl/l1-atomic/shared/quark-install/SKILL.md`.
